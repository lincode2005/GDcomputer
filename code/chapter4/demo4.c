#include <stdio.h>
int main()
{   
    int a;
    scanf("%d", &a);
    // 第一种写法
    // if(a>=0)
    // {
    //     printf("%d", a);
    // }

    // 第二种写法
    // if(a>=0)
    //     printf("%d", a);

    // 第三种写法
    if(a>=0) printf("%d", a);
    return 0;
}

